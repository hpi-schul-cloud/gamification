levels:
    type: manual
    steps: [100,200,360,500,900]

XPs: []

events: []

achievements:
    TestAchievement:
        #add this so rule parsing works
        requirements:
            - xp: XP
              amount: 0
    HiddenAchievement:
        #add this so rule parsing works
        requirements:
            - xp: XP
              amount: 0
        hidden: true
