XPs: []

events: []

achievements:
    TestAchievement:
        #add this so rule parsing works
        requirements:
            - xp:
                name: XP
                amount: 0
    HiddenAchievement:
        #add this so rule parsing works
        requirements:
            - xp:
                name: XP
                amount: 0
        hidden: true
